{
  "scripts": {
    "mini": "node src/mode/ap/minify.js && platformio run --target buildfs --environment miru && platformio run --target uploadfs --environment miru"
  },
  "dependencies": {
    "html-minifier": "^4.0.0"
  }
}
